% AUDIT01/T5.2 - generated derivation document (D-6: all twelve families)
% Witness source: verification/MAJORITY.json (executed Tue 25 Aug 2026 15:27:25)
\documentclass{article}
\usepackage{amsmath,amssymb}
\title{Gate family MAJORITY: closed-form one-set derivation}
\date{2026-08-25}
\begin{document}
\maketitle

\paragraph{Definition.} $g(x)=1$ iff $|\{i\in I_c:x_i=1\}|\ge t$, threshold $t=\lfloor d/2\rfloor+1$ (\texttt{tiePolicy}``\texttt{strict}'', ties$\to$0; D-3). At or above'' policy uses $t=\lceil d/2\rceil$.

\paragraph{Closed form.} $J=\bigcup_{S\subseteq I_c,\,|S|\ge t}\Bigl(\bigcap_{i\in S}B_i\cap\bigcap_{i\in I_c\setminus S}\overline{B}_i\Bigr)$: the union of Hamming layers at or above the threshold.

\paragraph{Ordering transport.} Hamming weight is preserved by $\varphi$, so layer unions are transport-invariant.
Canonical internal representation is LSB-first (weights $w(i)=2^{i-1}$);
transport to MSB row order exclusively via the involution $\varphi$
(\texttt{GOVERNANCE/ORDERING.md} \S2).

\paragraph{Mechanical witness (arity up to 6).} Executed elementwise equality
against the exhaustive truth-table repertoire:
\begin{center}
\begin{tabular}{lc}
\hline
Witness property & Value \\ \hline
Parameterisations swept & per \texttt{tools/t52_family_witnesses.wl} grid \\
Arities covered & 2, 3, 4, 5, 6 \\
Total cases & 5 \\
Elementwise failures & \textbf{0} \\
Maximum symmetric-difference size & 0 \\ \hline
\end{tabular}
\end{center}

\paragraph{Reproduce.} \texttt{WolframKernel -script tools/t52_family_witnesses.wl}
(regenerates every family's \texttt{verification/*.json}; exits non-zero on any failure).

\end{document}
