# CausalBool — root runtime dependencies.
#
# AUDIT03-C. There was NO manifest at root; only the four replication
# subprojects had one, so a clean checkout could not be installed and CI could
# not run at all.
#
# Direct dependencies only, pinned to the versions this programme's published
# numbers were produced with. Transitives are left to the resolver so the set
# installs on Linux runners as well as on the author's macOS.
#
# pybdm is pinned by src/description_lengths.py, which RAISES on any other
# version -- the BDM edge semantics differ per historical consumer and the
# published values depend on 0.1.0. Do not float it.

numpy==2.4.3
pandas==3.0.1
scipy==1.17.1
matplotlib==3.10.8
networkx==3.6.1
scikit-learn==1.8.0
seaborn==0.13.2
pybdm==0.1.0

# NOT pinned here, deliberately: 'requests' is imported only by
# src/integration/BulkScraper.py and grn_data_pipeline.py, whose entry points
# are orphans (audit/AUDIT03_R2_collapse/ORPHANS.md) and which reach external
# services. It is not installed in the working venv and no suite needs it, so
# pinning a version nobody has verified would be a fabricated constraint.
