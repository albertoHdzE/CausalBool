# Moved Method Derivations

The former `doc/causalBinpaper/` workspace has been promoted into the paper-programme layer.

Use the new canonical location instead:

- `papers/method/derivations/`

This signpost is kept so older notes and historical references remain intelligible.
