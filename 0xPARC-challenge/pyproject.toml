[build-system]
requires = ["setuptools==82.0.1"]
build-backend = "setuptools.build_meta"
[project]
name = "oxparc-challenge"
version = "1.0.0"
requires-python = ">=3.13,<3.14"
dependencies = ["numpy==2.4.3"]
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
